# 更新记录

本项目采用 [Semantic Versioning](https://semver.org/)。

## [2.1.1] - 2026-07-31

### 变更

- 将单 Skill 仓库扁平化，`SKILL.md`、`agents/`、`assets/`、`references/` 和 `scripts/` 直接位于仓库根目录。
- 将公开内容运行包归入 `examples/content-runs/`，将策略与三平台示例归入 `examples/strategy/`。
- 更新安装、运行、校验、测试和内部链接，不再依赖 `skill/wxyj-content-system` 双层路径。
- README 保留自然、清晰、可检索的项目信息，但不展示优化方法说明或仓库元数据操作建议。

### 验证

- 新增单 Skill 根目录结构与 README 文案边界回归测试。

## [2.1.0] - 2026-07-30

### 新增

- 内容运行脚本新增 `--product`，支持后续威士忌新品而不再写死首款产品。
- 校验器新增 manifest 与运行目录身份一致性、年月层级、状态、版本、平台重复和媒体归属检查。
- 校验器不再只检查文件存在，还会检查简报、来源和平台 QA 的关键内容是否为空。
- 内容运行脚本会在写入前拒绝产品冲突或新增未声明平台，避免形成半套目录。
- 风险表达校验可区分明确免责声明与正向收益承诺。
- 兼容内容包校验器新增 `--help`，并把只有标题、没有正文的空字段判定为缺失。
- 内容简报与视觉素材库新增可配置 `asset_root`，公开安装不再依赖单机绝对路径。

### 修复

- 删除小红书“同一轮8页”的旧规则，并清理示例中的9页内容。
- 调整首批日历为4–8页动态组合，降低长期生产的重复和冗余。
- 将本地 `dist/` 打包产物排除出 Git 提交范围。
- 新增跨平台文本换行规则，避免 Windows 提交造成无意义整文件差异。

## [2.0.0] - 2026-07-30

### 新增

- 中文名称统一为“威熏邑境自媒体内容生成系统”。
- Skill ID 统一为 `wxyj-content-system`。
- 采用仓库层与可安装 Skill 层分离的双层结构。
- 新增 `create_content_run.py`，创建标准内容运行目录。
- 新增 `validate_content_run.py`，检查目录、文件名、发布文案和风险表达。
- 小红书、抖音、视频号均强制交付标题、正文/描述、Tag、评论、CTA和披露。
- 新增 README 中英文版、开放说明、贡献指南、版本策略和内容目录文档。
- 新增日期、平台、主题、页/镜、角色与媒体版本命名体系。

### 变更

- 可发布内容不再以散落的图片和单一 Markdown 为交付单位，改为完整内容运行包。
- 小红书图片生成与发布正文不再分离。
- 旧 Skill ID `weixunyijing-content-ops` 保留但不再作为 V2 的主调用名称。

### 兼容

- 保留旧版内容包校验器；
- 不删除旧 Skill 或旧内容输出；
- 支持将旧内容复制到新运行目录。

## [1.2.0] - 2026-07-30

- 小红书改为按信息量动态选择3–8页。
- 增加封面、证据页、解释页、总结页等页面角色。
- 增加产品边缘软锁，减少白边、灰边和抠图光晕。
- 静态图文改为多模态模型直接生成设计文字。

## [1.1.0] - 2026-07-29

- 增加视频号版本，形成小红书、抖音、视频号三平台生产体系。
- 增加热点、节日、节气与常青内容适配规则。

## [1.0.0] - 2026-07-29

- 建立威熏邑境产品型内容运营 Skill。
- 建立产品事实、品牌定位、合规、平台、转化和复盘基础规则。
